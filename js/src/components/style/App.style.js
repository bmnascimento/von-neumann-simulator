const header = {
  margin: 0,
  padding: "1em",
}

const title = {
  margin: 0,
}

const main = {
  display: "flex",
  justifyContent: "center",
  flexGrow: 1,
}

export default { header, title, main }
